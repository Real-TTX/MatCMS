@page
@model MatCMS.Pages.Admin.Plugins.CreateModel
@{
    ViewData["Title"] = T["plugins.new"];
}
<div class="page-head">
    <h1>@T["plugins.new"]</h1>
    <a class="btn btn-sm btn-ghost" asp-page="Index">@T["action.back"]</a>
</div>

<div class="card">
    @if (!string.IsNullOrWhiteSpace(Model.Error))
    {
        <div class="alert alert-error">@Model.Error</div>
    }
    <form method="post" class="form-grid">
        <div class="form-field">
            <label for="Name">@T["plugins.name"]</label>
            <input id="Name" name="Name" value="@Model.Name" required autofocus />
            <div class="help">@T["plugins.nameHelp"]</div>
        </div>
        <div><button type="submit" class="btn">@T["plugins.createSubmit"]</button></div>
    </form>
</div>
